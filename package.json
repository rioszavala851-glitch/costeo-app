{
    "name": "costeo-app-root",
    "version": "1.0.0",
    "description": "Root package.json for deployment",
    "scripts": {
        "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
        "start": "npm start --prefix server",
        "dev": "echo \"Use 'npm run dev' inside client and server folders separately or install concurrently\""
    },
    "engines": {
        "node": ">=16.0.0"
    },
    "keywords": [],
    "author": "",
    "license": "ISC"
}